SCENARIO("When a Jet.IO.OperationDispatcher is sent a putHandlerOperations it initialises the correct no of handlers").
END();

SCENARIO("When a Jet.IO.OperationDispatcher is sent a new request that it can dispatch and has a handler available then it dispatches it").
END();

SCENARIO("When a Jet.IO.OperationDispatcher is sent a new request that it can dispatch and has no handler available then it queues it").
END();




SCENARIO("When a Jet instance is started up it reads configuration arguments from the command line").
END();

SCENARIO("When a Jet instance is started up it starts an HTTP Service").
END();

SCENARIO("When a Jet HTTP Service receives an HTTP request it returns a valid response").
END();

